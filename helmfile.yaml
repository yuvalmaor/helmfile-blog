repositories:
  - name: ingress-nginx
    url: https://kubernetes.github.io/ingress-nginx

environments:
  PROD:
  DEV:



releases:
  - name: ingress-nginx
    chart: ingress-nginx/ingress-nginx
    version: 4.12.0  # Specify the version you want to use
    namespace: ingress-nginx  # Specify the namespace where you want to deploy ingress-nginx
#    values:
#      - <values-file>.yaml  # Specify any values file if needed

  - name: my-release
    chart: ./blogchart
    version: "1.0.0"
    values:
      - {{ .Environment.Name }}/blogchart-values.yaml